meta {
  name: Get Pending Agents
  type: http
  seq: 1
}

get {
  url: http://localhost:8080/api/admin/agents/pending
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

vars:pre-request {
  token: {{token}}
}

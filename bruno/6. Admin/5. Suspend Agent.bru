meta {
  name: Suspend Agent
  type: http
  seq: 5
}

put {
  url: http://localhost:8080/api/admin/agents/1/suspend
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

vars:pre-request {
  token: {{token}}
}

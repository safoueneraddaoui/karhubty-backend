meta {
  name: Get Revenue Statistics
  type: http
  seq: 8
}

get {
  url: http://localhost:8080/api/admin/revenue
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

vars:pre-request {
  token: {{token}}
}

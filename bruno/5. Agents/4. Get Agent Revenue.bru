meta {
  name: Get Agent Revenue
  type: http
  seq: 4
}

get {
  url: http://localhost:8080/api/agents/1/revenue
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

vars:pre-request {
  token: {{token}}
}

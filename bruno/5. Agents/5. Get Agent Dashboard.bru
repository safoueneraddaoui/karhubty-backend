meta {
  name: Get Agent Dashboard
  type: http
  seq: 5
}

get {
  url: http://localhost:8080/api/agents/1/dashboard
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

vars:pre-request {
  token: {{token}}
}

meta {
  name: Get Unread Notifications
  type: http
  seq: 2
}

get {
  url: http://localhost:8080/api/notifications/unread
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

vars:pre-request {
  token: {{token}}
}

meta {
  name: Get All Notifications
  type: http
  seq: 1
}

get {
  url: http://localhost:8080/api/notifications
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

vars:pre-request {
  token: {{token}}
}

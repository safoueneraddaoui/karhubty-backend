meta {
  name: Mark All as Read
  type: http
  seq: 5
}

put {
  url: http://localhost:8080/api/notifications/read-all
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

vars:pre-request {
  token: {{token}}
}

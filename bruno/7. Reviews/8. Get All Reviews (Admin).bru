meta {
  name: Get All Reviews (Admin)
  type: http
  seq: 8
}

get {
  url: http://localhost:8080/api/reviews
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

vars:pre-request {
  token: {{token}}
}

meta {
  name: Get Pending Reviews (Admin)
  type: http
  seq: 9
}

get {
  url: http://localhost:8080/api/reviews/admin/pending
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

vars:pre-request {
  token: {{token}}
}

meta {
  name: Update Review
  type: http
  seq: 6
}

put {
  url: http://localhost:8080/api/reviews/1
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "rating": 4,
    "comment": "Good car, minor issues with AC"
  }
}

vars:pre-request {
  token: {{token}}
}

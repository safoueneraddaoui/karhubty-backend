meta {
  name: Get User Reviews
  type: http
  seq: 4
}

get {
  url: http://localhost:8080/api/reviews/user/{{userId}}
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

vars:pre-request {
  token: {{token}}
  userId: {{userId}}
}

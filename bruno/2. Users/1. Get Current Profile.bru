meta {
  name: Get Current User Profile
  type: http
  seq: 4
}

get {
  url: http://localhost:8080/api/users/profile
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

vars:pre-request {
  token: {{token}}
}

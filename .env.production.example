# Production Environment Configuration Template
# Save as .env.production in the backend directory
# IMPORTANT: Fill in all values with your actual production credentials

# ============================================
# DATABASE CONFIGURATION
# ============================================
# Use a strong, random password for production
DB_HOST=postgres
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=change-to-strong-secure-password
DB_DATABASE=karhubty

# ============================================
# JWT AUTHENTICATION
# ============================================
# Generate a strong random secret (min 32 characters)
# Command: openssl rand -hex 32
JWT_SECRET=your-production-jwt-secret-key-min-32-characters

# ============================================
# APPLICATION SETTINGS
# ============================================
PORT=8080
NODE_ENV=production
UPLOAD_PATH=./uploads
MAX_FILE_SIZE=10485760

# ============================================
# EMAIL CONFIGURATION - BREVO
# ============================================
# Get these from your Brevo account:
# 1. Go to https://www.brevo.com
# 2. Log in or sign up
# 3. Go to Settings → SMTP & API
# 4. Copy your credentials

EMAIL_PROVIDER=brevo
EMAIL_HOST=smtp-relay.brevo.com
EMAIL_PORT=587

# Your Brevo account email address
BREVO_EMAIL=your-email@example.com

# Your Brevo SMTP key (generate in Brevo dashboard)
# Settings → SMTP & API → Generate an SMTP key
BREVO_SMTP_KEY=xkeysib-your-actual-smtp-key-here

# Must be a verified sender in Brevo
# Go to Senders → Add your domain or email
EMAIL_FROM=noreply@yourdomain.com

# ============================================
# FRONTEND CONFIGURATION
# ============================================
# Your production domain URL
FRONTEND_URL=https://your-production-domain.com

# ============================================
# OPTIONAL: Docker-Compose Variables
# ============================================
# For Docker deployments, also create:

DB_PASSWORD=change-to-strong-secure-password
JWT_SECRET=your-production-jwt-secret-key-min-32-characters
BREVO_EMAIL=your-email@example.com
BREVO_SMTP_KEY=xkeysib-your-actual-smtp-key-here
EMAIL_FROM=noreply@yourdomain.com
FRONTEND_URL=https://your-production-domain.com
REACT_APP_API_URL=https://api.your-production-domain.com
